# Generated per session
compose/.env

# Python virtualenvs
.venv/
__pycache__/

# Build artifacts
*.egg-info/
target/

# Metric-coverage gate snapshot: catalog_metrics.json, written by
# lib/collect_metrics.py while the suite runs and consumed by the
# metric-coverage-gate job. The OpenAPI spec-drift gate generates offline and
# commits nothing here. Also used for ad-hoc local dumps.
.artifacts/

# pytest cache
.pytest_cache/
