apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "insight-analytics.fullname" . }}
  labels:
    {{- include "insight-analytics.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "insight-analytics.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "insight-analytics.selectorLabels" . | nindent 8 }}
      annotations:
        # Roll pods when the gears config changes.
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global }}
      {{- with .imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      # Suppress the legacy `<SVC>_SERVICE_HOST/PORT/…` Docker-link env-vars
      # kubelet auto-injects for every Service in the namespace at pod-start
      # (see api-gateway deployment for the longer explanation).
      enableServiceLinks: false
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: analytics
          image: "{{ required "image.repository is required" .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # gears-rust host: structural config from the mounted ConfigMap;
          # leaf values (DB/tenant) from the Secret via envFrom below.
          command: ["/app/analytics"]
          args: ["-c", "/app/config/analytics.yaml", "run"]
          volumeMounts:
            - name: analytics-config
              mountPath: /app/config/analytics.yaml
              subPath: analytics.yaml
              readOnly: true
            {{- if .Values.gateway.caCertSecret }}
            # Discovery-issuer CA for the oidc-authn-plugin's https JWKS fetch.
            - name: authn-ca
              mountPath: /etc/insight/authn-ca
              readOnly: true
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          # Gateway-JWT verification (NGINX_BFF R1): the plugin's verification
          # config is nested/list-shaped and rendered into the ConfigMap from
          # `.Values.gateway.*` (issuer / discoveryUrl / audience / caCertPaths),
          # so there are no APP__ env overrides for it here.
          envFrom:
            # Operator MUST pre-create this Secret (umbrella does it
            # automatically as `insight-analytics-config`). Provides the
            # APP__gears__analytics__config__* leaf overrides.
            - secretRef:
                name: {{ required "existingSecret is required (umbrella provides `insight-analytics-config`; standalone installs supply their own)" .Values.existingSecret | quote }}
          securityContext:
            allowPrivilegeEscalation: false
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      initContainers:
        - name: migrate
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/app/analytics"]
          args: ["-c", "/app/config/analytics.yaml", "migrate"]
          volumeMounts:
            - name: analytics-config
              mountPath: /app/config/analytics.yaml
              subPath: analytics.yaml
              readOnly: true
          envFrom:
            - secretRef:
                name: {{ .Values.existingSecret | quote }}
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: analytics-config
          configMap:
            name: {{ include "insight-analytics.fullname" . }}-gears-config
        {{- if .Values.gateway.caCertSecret }}
        - name: authn-ca
          secret:
            secretName: {{ .Values.gateway.caCertSecret | quote }}
            items:
              - key: ca.crt
                path: ca.crt
        {{- end }}
