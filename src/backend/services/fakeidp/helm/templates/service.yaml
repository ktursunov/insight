apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-fakeidp.fullname" . }}
  labels:
    {{- include "insight-fakeidp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insight-fakeidp.selectorLabels" . | nindent 4 }}
