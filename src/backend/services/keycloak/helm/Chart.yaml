apiVersion: v2
name: insight-keycloak
description: Keycloak dev/e2e IdP (real-OIDC counterpart to fakeidp) for the nginx+auth stack — LOCAL/CI only, never production
version: 0.1.0
appVersion: "26.4"
type: application
