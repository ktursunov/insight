{{- if .Values.sessionKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "claude-team-proxy.fullname" . }}
  labels:
    {{- include "claude-team-proxy.labels" . | nindent 4 }}
type: Opaque
stringData:
  SESSION_KEY: {{ .Values.sessionKey | quote }}
{{- end }}
